export interface CreationCard {
  description: string;
  comingSoon?: boolean;
  imageUrl: string;
  title: string;
}

export interface NavItem {
  href: string;
  label: string;
}

export interface StudioValue {
  description: string;
  icon: string;
  title: string;
}